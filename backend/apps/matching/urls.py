"""
URL configuration for matching app.
"""
from django.urls import path

app_name = 'matching'

urlpatterns = [
    # 매칭 관련 URL 패턴을 여기에 추가
    # views.py 파일 생성 후 사용
    # 예: path('', views.matching_list, name='matching_list'),
]

